# 项目技术架构与详细说明

> 本文档详细介绍项目使用的技术栈、各模块功能、设计决策、以及未来可优化的方向。用于学习时理解整体设计，以及作为扩展开发的参考。

---

## 一、技术栈全景

### 1.1 后端技术栈

| 技术/库 | 用途 | 选型理由 |
|---------|------|----------|
| **Python 3.11** | 开发语言 | AI生态最完善的语言，LangChain等核心库原生支持 |
| **FastAPI** | Web框架 | 现代、高性能、自动生成API文档、类型提示友好，学习曲线平缓 |
| **LangChain** | LLM应用框架 | 目前最主流的LLM应用开发框架，封装了各种模型和工具 |
| **ChromaDB** | 向量数据库 | 轻量级、本地文件存储、无需额外部署，完美适合原型和学习 |
| **Pydantic v2** | 数据验证 | FastAPI原生集成，类型安全，自动生成API文档 |
| **pypdf** | PDF解析 | 轻量PDF文档加载器 |
| **Unstructured** | 文档处理 | 支持多种文档格式的解析 |

### 1.2 前端技术栈

| 技术/库 | 用途 | 选型理由 |
|---------|------|----------|
| **HTML5 + 原生JS** | 基础架构 | 零构建步骤，打开即用，降低学习门槛 |
| **Tailwind CSS (CDN)** | 样式框架 | 无需编译，class名直接写样式，快速构建美观界面 |
| **Fetch API** | 网络请求 | 浏览器原生支持，无需额外库 |

> **为什么不用React/Vue？** 前端不是本项目的学习重点。用最轻量化的方案，可以把注意力集中在AI核心逻辑上。如果后续需要复杂交互再考虑升级。

### 1.3 部署与运维

| 技术 | 用途 |
|------|------|
| Docker | 容器化部署 |
| Docker Compose | 多服务编排 |
| Nginx | 静态文件服务 + API反向代理 |

### 1.4 支持的LLM平台

所有支持 **OpenAI 兼容接口** 的大模型平台都可以接入：

| 平台 | 代表模型 | 适合场景 |
|------|----------|----------|
| 火山方舟（字节跳动） | 豆包系列 | 国内首选，质量好、速度快、企业级支持 |
| 通义千问（阿里） | qwen-plus / qwen-max | 阿里云用户首选 |
| 文心一言（百度） | ERNIE-4.0 | 百度云用户首选 |
| DeepSeek | deepseek-chat | 性价比高，开发者友好 |
| 智谱AI | GLM-4 | 国产老牌，技术积累深 |
| OpenAI | GPT-4o / GPT-4 Turbo | 国际标杆，能力最强（需海外网络） |

---

## 二、核心功能模块详解

### 2.1 对话问答模块（Chat）

**功能：** 最基础的LLM对话能力，支持多轮对话历史。

**核心文件：**
- `api/chat.py` - 对话接口
- `services/llm_service.py` - LLM调用封装

**工作流程：**
```
前端发送消息列表
    ↓
Chat API 接收请求
    ↓
转换为 LangChain Message 格式
    ↓
调用 LLM Service
    ↓
通过 LangChain 调用大模型 API
    ↓
返回回复内容
```

**设计要点：**
- 对话历史完全由前端维护（简化后端，无状态）
- LLM调用统一封装，方便切换提供商
- 使用 LangChain 抽象层，底层模型切换不影响业务代码

### 2.2 RAG知识库问答模块

**功能：** 基于上传的文档回答问题，附带引用来源。

**核心文件：**
- `api/rag.py` - RAG接口
- `services/rag_service.py` - RAG核心逻辑
- `services/document_service.py` - 文档处理

**RAG 完整工作流：**

```
文档上传阶段：
    文档 → 加载解析 → 文本切分 → 生成Embedding → 存入向量库

问答阶段：
    用户问题 → 生成问题Embedding → 向量相似度检索 → 获取Top-K相关片段
         ↓
    构建Prompt（系统提示 + 检索到的上下文 + 用户问题）
         ↓
    调用LLM生成回答
         ↓
    返回答案 + 引用来源
```

**关键设计决策：**

1. **切分策略：** 使用 RecursiveCharacterTextSplitter
   - 按字符递归切分，尽量保持语义完整
   - 支持中文优化的分隔符（句号、感叹号、问号等）
   - 重叠部分保证上下文连贯性

2. **检索策略：** 纯向量相似度检索
   - 简单直接，适合入门学习
   - 生产环境通常还会加上：关键词检索混合、重排序（Rerank）等

3. **向量化：** 使用平台提供的Embedding模型
   - 与LLM同平台的Embedding通常配合更好
   - 注意维度一致性（不同模型维度不同，不能混用）

### 2.3 Agent智能助手模块

**功能：** AI自主决策调用工具来解决复杂问题。

**核心文件：**
- `api/agent.py` - Agent接口
- `services/agent_service.py` - Agent核心逻辑

**Agent 工作原理（ReAct模式）：**

```
用户问题
    ↓
┌─→ Thought（思考）：分析问题，决定下一步行动
│       ↓
│   Action（行动）：选择工具并调用
│       ↓
│   Observation（观察）：获取工具返回结果
│       ↓
└── 继续思考？ → 是 → 重复上述循环
        ↓ 否
    给出最终答案
```

**已实现的三个工具：**

| 工具名称 | 功能 | 触发场景 |
|---------|------|----------|
| 知识库查询 | 检索IT知识库文档 | 技术问题、操作步骤、故障排查 |
| 工单查询 | 查询IT工单状态 | 用户问工单号、工单进度时 |
| 常见问题检索 | FAQ快速问答 | 重复性高的常见问题 |

**设计要点：**

1. **工具描述（description）是关键**
   - Agent完全靠工具描述来判断什么时候用什么工具
   - 描述要清晰、准确、包含适用场景
   - 这本质上就是给AI看的"产品说明文档"

2. **ReAct提示词模板**
   - 明确定义了思考→行动→观察的格式
   - 给出了具体的格式示例
   - 约束了输出格式，保证可解析性

3. **最大迭代次数限制（max_iterations=5）**
   - 防止Agent陷入无限循环
   - 生产环境需要根据场景调整

### 2.4 文档管理模块

**功能：** 文档的上传、列表、删除、向量化。

**核心文件：**
- `api/documents.py` - 文档管理接口
- `services/document_service.py` - 文档处理服务

**支持的格式：**
- PDF（.pdf）
- 纯文本（.txt）
- Markdown（.md）

---

## 三、代码结构与设计模式

### 3.1 分层架构

```
┌──────────────────────────────┐
│    API Layer (路由层)         │  app/api/*.py
│  - 接收HTTP请求               │
│  - 参数验证                   │
│  - 返回响应                   │
├──────────────────────────────┤
│    Service Layer (服务层)     │  app/services/*.py
│  - 业务逻辑                   │
│  - 调用外部服务               │
│  - 数据处理                   │
├──────────────────────────────┤
│    Model Layer (模型层)       │  app/models/*.py
│  - 数据结构定义               │
│  - 类型验证                   │
├──────────────────────────────┤
│    Config Layer (配置层)      │  app/config.py
│  - 环境变量加载               │
│  - 全局配置管理               │
└──────────────────────────────┘
```

### 3.2 设计模式

1. **单例模式（Singleton）**
   - 每个Service都有全局单例实例
   - 通过 `get_xxx_service()` 函数获取
   - 避免重复初始化，节省资源

2. **依赖倒置（Dependency Inversion）**
   - 业务逻辑依赖抽象（LangChain接口）
   - 切换底层LLM提供商不影响业务代码
   - 通过配置而非硬编码选择实现

3. **分层职责分离**
   - API层只管HTTP，不含业务逻辑
   - Service层只管业务，不感知HTTP
   - 层之间通过数据模型传递信息

### 3.3 关键设计决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 前端方案 | 纯HTML+JS，零构建 | 降低学习门槛，聚焦AI核心 |
| 向量库 | Chroma本地存储 | 无需额外部署，开箱即用 |
| LLM接入 | OpenAI兼容模式 | 通用性最强，切换平台成本最低 |
| 对话状态 | 前端维护，后端无状态 | 简化后端，适合教学项目 |
| 文档元数据 | 文件名存储，无数据库 | 减少依赖，快速上手 |
| Agent框架 | LangChain ReAct | 最经典、最易理解的Agent模式 |

---

## 四、未来可优化与扩展方向

> 按从易到难排序，可作为学习进阶的练习项目。

### 🟢 入门级（1-3天可完成）

1. **修改系统提示词，定制助手性格**
   - 文件：`llm_service.py` / `rag_service.py` 中的 system prompt
   - 练习：让助手用不同的语气、身份回答问题
   - 学习点：Prompt工程基础

2. **添加更多FAQ条目**
   - 文件：`agent_service.py` 中的 `_tool_faq_search` 方法
   - 练习：增加你自己领域的常见问题
   - 学习点：理解工具如何影响Agent能力

3. **调整RAG参数，观察效果变化**
   - 参数：`chunk_size`、`chunk_overlap`、`rag_top_k`
   - 练习：用同一个问题，不同参数，对比回答质量
   - 学习点：理解RAG各参数的作用

4. **添加新的Agent工具**
   - 文件：`agent_service.py` 的 `_create_tools()` 方法
   - 练习：添加一个新工具（如计算器、天气查询、日期查询）
   - 学习点：理解工具描述的重要性、Agent决策逻辑

5. **美化前端界面**
   - 文件：`frontend/` 目录下所有文件
   - 练习：修改配色、布局、添加动画效果
   - 学习点：前端基础、用户体验

### 🟡 进阶级（1-2周可完成）

6. **实现流式输出（SSE）**
   - 将回答改为逐字流式返回
   - 后端：FastAPI StreamingResponse
   - 前端：EventSource / fetch + ReadableStream
   - 学习点：流式API、LLM streaming

7. **添加对话历史持久化**
   - 目前对话历史仅保存在浏览器内存
   - 可选择方案：LocalStorage / SQLite / Redis
   - 学习点：状态管理、数据持久化

8. **支持更多文档格式**
   - Word (.docx)、Excel、PPT、网页等
   - 使用 LangChain 的各种 Document Loader
   - 学习点：文档处理生态

9. **添加Rerank重排序阶段**
   - 在向量检索后增加一步重排序
   - 使用 Cohere Rerank 或 平台自带的rerank接口
   - 显著提升RAG回答质量
   - 学习点：RAG优化进阶

10. **多用户支持与认证**
    - 添加用户登录注册
    - 每个用户有独立的知识库和对话历史
    - 方案选择：JWT / Session / OAuth
    - 学习点：Web安全、用户系统

11. **对话评价与反馈功能**
    - 在每条回答旁边加 👍/👎 按钮
    - 收集用户反馈用于优化
    - 学习点：产品迭代闭环、数据驱动优化

### 🔴 高级（1个月+）

12. **生产级向量数据库**
    - 替换 Chroma 为 Milvus / Qdrant / Weaviate
    - 支持更大数据量、更高并发
    - 学习点：向量数据库架构、性能优化

13. **多Agent协作系统**
    - 不止一个Agent，而是多个专业Agent
    - 例如：客服Agent + 技术专家Agent + 管理员Agent
    - 学习点：Multi-Agent系统设计、任务分发

14. **知识库自动同步**
    - 对接企业Wiki、Confluence、飞书文档等
    - 定时同步、增量更新
    - 学习点：系统集成、ETL流程

15. **评估与评测体系**
    - 建立RAG和Agent的量化评测指标
    - 自动化评测流水线
    - 学习点：AI质量保障、评测方法论

16. **成本监控与优化**
    - Token用量统计和费用追踪
    - 缓存策略（相似问题直接返回缓存答案）
    - 学习点：AI应用经济性、工程优化

17. **部署到生产环境**
    - 云服务器部署、域名配置
    - HTTPS、负载均衡
    - 监控告警（Prometheus + Grafana）
    - 学习点：DevOps、生产环境运维

---

## 五、学习重点建议

### 5.1 你的核心优势路线

基于你20年的项目管理经验，建议走 **「资深PM + AI落地操盘手」** 的差异化路线：

| 能力领域 | 投入占比 | 目标深度 | 说明 |
|----------|---------|----------|------|
| AI核心原理 | 40% | 深 | RAG、Agent、Prompt工程——这是你的核心武器 |
| AI项目管理 | 25% | 深 | 需求拆解、场景评估、ROI分析、风险管控——你的长板+AI |
| 工程实现 | 20% | 中 | 能读懂代码、能做原型、能评估技术方案 |
| 前端/UI | 10% | 浅 | 知道概念、能改就行，AI能帮你搞定 |
| 底层算法 | 5% | 了解 | 知道大概原理即可，不需要从头实现 |

### 5.2 各模块学习优先级

**必须精通（决定你的AI认知天花板）：**
- 🎯 RAG的原理与调优方法论
- 🎯 Agent设计模式与应用场景
- 🎯 Prompt工程的核心原则
- 🎯 LLM的能力边界与常见误区

**需要理解（决定你和技术团队的沟通效率）：**
- 📖 向量数据库的工作原理
- 📖 Embedding的本质与作用
- 📖 整体架构与数据流
- 📖 API设计与前后端交互

**了解即可（AI和工程师可以帮你搞定）：**
- 📚 Python语法细节
- 📚 前端框架和CSS技巧
- 📚 数据库SQL优化
- 📚 底层神经网络原理

### 5.3 学习方法建议

1. **以用带学，不要等"准备好"**
   - 先有一个具体的应用场景/问题
   - 用AI快速搭出原型
   - 边用边学边优化

2. **从修改开始，不要从空白开始**
   - 先看懂别人的代码
   - 从小修改开始（改个提示词、加个工具）
   - 逐步加深理解

3. **建立你的"AI工具箱"**
   - 每个解决过的问题都记录下来
   - 每个好用的Prompt都保存好
   - 每个踩过的坑都总结一下
   - 这会成为你最值钱的资产

4. **关注落地，而非纯技术**
   - 技术方案能不能解决业务问题？
   - 成本和收益比怎么样？
   - 风险在哪里？
   - 这才是PM的核心价值所在

---

## 六、常见技术概念速查

| 概念 | 一句话解释 |
|------|-----------|
| **LLM** | 大语言模型，比如GPT、豆包、通义千问等 |
| **Embedding** | 把文本变成一组数字（向量），用来计算语义相似度 |
| **向量数据库** | 专门存向量的数据库，支持按语义相似度快速检索 |
| **RAG** | 检索增强生成，先搜资料再回答，让AI基于特定知识回答 |
| **Agent** | 智能体，AI能自主调用工具来完成任务，而不只是聊天 |
| **ReAct** | Agent的经典工作模式：推理(Reasoning) + 行动(Acting) |
| **Prompt工程** | 如何写好提示词，让AI给出更好的结果 |
| **Token** | LLM计算用量的单位，大致1Token≈0.7个汉字 |
| **Fine-tuning** | 微调，用特定数据继续训练模型，让它更适配某个领域 |
| **Function Calling** | 函数调用，让LLM输出结构化的工具调用指令 |
| **Chunker** | 文档切分器，把长文档切成适合向量化的小片段 |
| **Top-K** | 检索时返回最相关的前K个结果 |

---

> 💡 **记住：** AI领域技术更新很快，工具和框架会变，但核心思想（RAG的检索增强、Agent的工具调用、Prompt的指令遵循）是相对稳定的。把原理吃透，工具只是实现手段。
